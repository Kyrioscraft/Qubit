# Cloudflare Pages 部署配置

# 构建命令
command = "npm run build"

# 输出目录
publish = ".next"

# 环境变量（如需要）
# [vars]
# NEXT_PUBLIC_SITE_URL = "https://qubit.pages.dev"

# 函数配置（用于 API 路由）
[functions]
node_compat = true